<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>AMApp</title>

        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.2.14/semantic.min.css"></link>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.2.14/semantic.min.js"></script>
    </head>

    <body>
        <div class="ui inverted segment">
            <div class="ui inverted secondary pointing menu">
                <a id="home" class="item" href="/">
                    <i class="home icon"></i>Home
                </a>
                <a id="contracts" class="item" href="/contracts">
                    <i class="leaf icon"></i>Contracts
                </a>
                <a id="bulk" class="item" href="/bulk">
                    <i class="food icon"></i>Bulk
                </a>
                <% if( !currentUser ) { %>
                    <a id="login" class="item right" href="/login">Login</a>
                    <a id="register" class="item" href="/register">Sign Up</a>
                <% } else { %>
                    <a class="item right" href="#">
                        Connected as <%= currentUser.username %>
                        <% if( currentUser.isAdmin ) { %>
                            (admin)
                        <% } else if( currentUser.isProducer ) { %>
                            (producer)
                        <% } %>
                    </a>
                    <a class="item" href="/logout">Logout</a>
                <% } %>
            </div>
        </div>